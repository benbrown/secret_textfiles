<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="{{rootUrl}}/css/main.css" />
    <link rel="stylesheet" href="{{rootUrl}}/css/secret.css" />
  </head>
  <body>
    <div id="main">
      <h1>Secret Control Panel</h1>
      <nav class="toolbar">
        <a href="{{rootUrl}}/secret">All Posts</a>
        <a href="{{rootUrl}}/secret/new">New Post</a>
      </nav>

      {{{body}}}
    </div>
  </body>
</html>
