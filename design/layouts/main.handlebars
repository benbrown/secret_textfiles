<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="{{rootUrl}}/css/main.css" />
  </head>
  <body>
    <div id="main">
      <header>
        <a href="{{rootUrl}}/">{{title}}</a>
      </header>

      {{{body}}}

    </div>
  </body>
</html>
