<h4>└ <a href="{{rootUrl}}/stream/{{post.metadata.date}}">{{post.metadata.date}}</a> /</h4>
<article>
<h4>{{post.metadata.title}}</h4>
<div class="post-content">
{{{post.rendered}}}
</div>
</article>
